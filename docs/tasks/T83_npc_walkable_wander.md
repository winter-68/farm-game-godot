# T83：NPC 可行走随机路线

## 目标

修复 NPC 随机目标点落进建筑/水池碰撞区后，一直对着墙走的问题。

## 实现

1. NPC 选择随机目标点时，先检测该点是否可行走。
2. 如果候选点被碰撞体占用，则重新随机。
3. 如果 NPC 移动中持续卡墙，则放弃当前目标，等待后重新选路。
4. 调整两个村民的默认活动区域，让他们优先在村口空地活动。

## 验收

- NPC 不再长时间对着墙走。
- NPC 会在空地中随机走动。
- 对话提示和对话交互仍然正常。
